<% @content_for_tools << link_to('list', { :action => 'index' }, :class => 'house' ) %>

<% if block %>
  <% @content_for_tools << link_to('show', { :action => 'show', :id => block.id }) %>
  <% @content_for_tools << link_to('edit', { :action => 'edit', :id => block.id }) %>
  <% @content_for_tools << link_to('delete', { :action => 'delete', :id => block.id }, :confirm => 'Are you sure that you want to delete this block?') %>

  <% @content_for_tools << link_to('edit links', { :action => 'edit_links', :id => block.id }) %>
<% end %>
