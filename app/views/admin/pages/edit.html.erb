<%= render :partial => 'top_links', :locals => { :page => @page } %>

<h1><%= I18n.t('page.update.action').capitalize %></h1>

<% if @page %>
  <%= render :partial => 'form', :locals => { :page => @page, :sections => @sections } %>
<% end %>
