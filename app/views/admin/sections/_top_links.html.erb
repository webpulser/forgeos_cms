<% @content_for_tools << link_to('list', { :action => 'index' }, :class => 'house' ) %>

<% if section %>
  <% @content_for_tools << link_to('show', { :action => 'show', :id => section.id }) %>
  <% @content_for_tools << link_to('edit', { :action => 'edit', :id => section.id }) %>
  <% @content_for_tools << link_to('delete', { :action => 'delete', :id => section.id }, :confirm => 'Are you sure that you want to delete this section?') %>
<% end %>
