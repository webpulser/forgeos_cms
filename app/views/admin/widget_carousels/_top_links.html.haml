- content_for :tools, link_to(I18n.t('list').capitalize, admin_widget_carousels_path, :class => 'house' )
- if carousel && !(carousel.new_record?)
  - content_for :tools do
    = link_to(I18n.t('show').capitalize, admin_widget_carousel_path(carousel))
    = link_to(I18n.t('edit').capitalize, edit_admin_widget_carousel_path(carousel))
    = link_to(I18n.t('delete').capitalize, admin_widget_carousel_path(carousel), :confirm => I18n.t('carousel.destroy.confirm').capitalize, :method => :delete)
-#    = link_to(I18n.t('item.create.action').capitalize, { :action => 'create_item', :carousel_id => carousel.id })
