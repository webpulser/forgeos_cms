<% content_for :meta_keywords do %><%= @page.keywords if @page %><% end %>
<% content_for :meta_description do %><%= @page.description if @page %><% end %>

<% content_for :title do %>
 - <%= @page ? @page.title : 'Page not found' %>
<% end %>

<!--content-->
<div id="content">
  <% if @page %>
    <div id="page_<%= @page.id %>">
      <%= @page.content %>
    </div>

  <% else %>
    <%= I18n.t('page_not_found').capitalize %>
  <% end %>

  <% if @blocks && !@blocks.empty? %>
    <% @blocks.each do |block| %>
      <div id="block_<%= block.id %>"><%= block.content %></div>
    <% end %>
  <% end %>
</div>
<!--content-end-->
