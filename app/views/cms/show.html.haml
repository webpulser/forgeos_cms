- if @page && @page.meta_info
  - content_for :title, @page.meta_info.title
  - content_for :description, @page.meta_info.description
  - content_for :keywords, @page.meta_info.keywords
.before_breadcrumbs
.breadcrumbs
  .inner-breadcrumbs
    %a{ :href => "#" }
      Accueil
    >
    = @page.title if @page
.inner-content
  .cart-step-content
    - if @page
      %div{ :id => "page_#{@page.id}" }
        = @page.content
      - if @blocks && !@blocks.empty?
        - @blocks.each do |block|
          %div{ :id => "block_#{block.id}" }
            = block.content

      = render :partial => 'cms/linked_pages', :locals => { :linked_pages => @page.linked_pages }

    - else
      = I18n.t('page_not_found').capitalize
